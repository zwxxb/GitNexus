"""Settings for the subdir Django project. Module path is `myproj.settings`,
resolved relative to manage.py's directory (backend/), not the repo root."""

DEBUG = True
ROOT_URLCONF = 'myproj.urls'
INSTALLED_APPS = ['app']
